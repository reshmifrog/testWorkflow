name: Test Monitor Issues

on:
  workflow_dispatch:
    inputs:
      operations-per-run:
        description: 'Number of operations per run'
        required: false
        default: '30'
  schedule:
    - cron: "0 0 * * *"  # Runs once a day at midnight

jobs:
  workflow_call:
    uses: reshmifrog/.github/actions/monitor-issues@a197af79b89bf5186b2b3304dd09bf844083e897
    with:
      operations-per-run: 30
